---
import CTA from '@/components/sections/CTA.astro';
import Footer from '@/components/footer.astro';
import BaseHead from '@/components/base-head.astro';
import TailwindIndicator from '@/components/tailwind-indicator.astro';
import ThemeToggle from '@/components/theme-toggle.astro';
import { cn } from '@/lib/utils'; // Assuming you have a utility for merging classes like in shadcn/ui

interface Props {
  title: string;
  description: string;
  image?: string;
  ogType?: string;
  noindex?: boolean;
  nofollow?: boolean;
  twitterCreator?: string;
  twitterSite?: string;
  hideCTA?: boolean;
  schema?: {
    type: string;
    date?: Date;
    author?: string;
    tags?: string[];
  };
}

const { title, description, image, ogType, schema, noindex, nofollow, twitterCreator, twitterSite, hideCTA } =
  Astro.props;
---

<!doctype html>
<html lang='en'>
  <head>
    <BaseHead
      title={title}
      description={description}
      image={image}
      ogType={ogType}
      noindex={noindex}
      nofollow={nofollow}
      twitterCreator={twitterCreator}
      twitterSite={twitterSite}
      schema={schema}
    />
  </head>
  <body class='bg-background min-h-screen font-sans antialiased'>
    <header class='bg-card border-b'>
      <div class='container mx-auto flex flex-col items-center px-4 pt-8 pb-4 text-center'>
        <div class='mb-2 flex w-full items-center justify-center'>
          <h1
            class='from-primary via-chart-2 to-chart-3 bg-gradient-to-r bg-clip-text text-4xl font-extrabold tracking-tight text-transparent drop-shadow-sm'
          >
            DeepReads
          </h1>
          <div class='ml-3'>
            <ThemeToggle />
          </div>
        </div>
        <p class='text-muted-foreground mb-6 text-lg'>Your daily digest of what matters</p>
        <nav class='flex justify-center'>
          <div class='bg-muted flex gap-2 rounded-full p-1 shadow-sm'>
            <a
              href='/'
              class={cn(
                'rounded-full px-5 py-2 text-base font-medium transition-colors',
                !Astro.url.pathname.startsWith('/tags')
                  ? 'bg-primary text-primary-foreground shadow'
                  : 'text-secondary-foreground hover:bg-accent hover:text-accent-foreground',
              )}
            >
              Reads
            </a>
            <a
              href='/tags/'
              class={cn(
                'rounded-full px-5 py-2 text-base font-medium transition-colors',
                Astro.url.pathname.startsWith('/tags')
                  ? 'bg-primary text-primary-foreground shadow'
                  : 'text-secondary-foreground hover:bg-accent hover:text-accent-foreground',
              )}
            >
              Tags
            </a>
          </div>
        </nav>
      </div>
    </header>
    <main class='container mx-auto px-4 py-8'>
      <slot />
    </main>
    {!hideCTA && <CTA />}
    <Footer />
    <TailwindIndicator />
  </body>
</html>
